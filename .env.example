# Secrets only. Copy to .env (gitignored). Runtime settings live in the
# database, not here — see CLAUDE.md rule 8.

DATABASE_URL=postgresql+psycopg2://swing:swing@localhost:5433/markov_trader
TEST_DATABASE_URL=postgresql+psycopg2://swing:swing@localhost:5433/markov_trader_test
JWT_SECRET=change-me-in-production
# Encrypts per-user BYO LLM API keys at rest (backend/api/encryption.py).
# Dev-only value below — generate a real one for production with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=gaO00p7mU3YKkD25WbYhMTe3OXfJHAIDJC0MnKWm084=
